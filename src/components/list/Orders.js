import React, { Component } from 'react';

export default class OrderList extends Component {
    render(){
        // let orderList = []
        // if (user && this.props.orders){            
        //     let list = this.props.orders.map((o, i) => {
        //     if (o.email) {
        //         if (o.email === user.email && !o.delete) {
        //             return(
        //                 <div className="user-show-recipe">
        //                 <button className={this.state.remove ? "deleteBtn" : "hide-delete deteleBtn"} value={o.id} onClick={this.deleteThis}>Delete</button>
        //                 <form className="linkBtn" key={i}>
        //                     <button  className="linkBtn" type="button" name="recipe" value={o.id} onClick={(e) => {showThisRecipe(e)}}>
        //                     <div className="recipe-img">FUTURE PICTURE</div>

        //                     <div className="recipe-data">
        //                         <section>{o.style}</section>
        //                     </div>

        //                     </button>
        //                 </form>
        //                 </div>
        //             )
        //         }
        //     }
        //     })
        //     orderList.push(list)
        // }
        return(
            <></>
        )
    }
}    